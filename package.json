{
  "name": "epir-art-jewellery",
  "private": true,
  "workspaces": [
    "extensions/*",
    "worker",
    "hydrogen"
  ],
  "scripts": {
    "test:e2e": "playwright test",
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "hydrogen:dev": "cd hydrogen && npm run dev",
    "hydrogen:deploy": "cd hydrogen && npm run deploy"
  }
  ,
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
